# Razorpay Test Mode keys (never commit real values — Test Mode only)
RZP_KEY_ID=rzp_test_xxxxxxxxxxxxxx
RZP_KEY_SECRET=xxxxxxxxxxxxxxxxxxxx
RZP_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# Local mandate signing key (generate: openssl rand -hex 32)
MANDATE_HMAC_KEY=

# LLM provider (OpenAI-compatible) — optional; deterministic stub is the default
OPENAI_BASE_URL=
OPENAI_API_KEY=
LLM_MODEL=
